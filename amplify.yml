version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
        # - yum -y install java-1.8.0-openjdk-devel
        # - yum -y install lsof
    build:
      commands:
        - yarn run test && yarn run build
  artifacts:
    baseDirectory: build
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
